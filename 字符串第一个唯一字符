  int[] dic = new int[26];

            for(int i = 0;i < s.Length; i++)
                dic[s[i]-'a']++;
            

            for (int i = 0; i < s.Length; i++)
                if (dic[s[i] - 'a'] == 1)
                    return i;
            return -1;
  
